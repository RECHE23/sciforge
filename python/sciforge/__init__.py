"""SciForge's shared Python infrastructure (development-only, never shipped).

Currently the :mod:`sciforge.bench` benchmark substrate. Consumed from a sibling checkout
via ``PYTHONPATH=../sciforge/python`` — the same convention as the shared ``include/`` and
``lint/`` directories.
"""
